package Homework.studysunday;

import Homework.SOCRATİVE;
import jdk.dynalink.beans.StaticClass;

import java.util.Scanner;

public class monday {
    public static void main(String[] args) {
      Scanner scanner = new Scanner(System.in);

/*

//Soru 17:Girilen kenar uzunluklarına göre üçgen oluşturup oluşturmadığını eğer oluşturuyorsa çevre ve alanını ekrana yazan programı Java dilinde yazınız.

        System.out.println("üçgenin 1.kenarını giriniz:");
        int a=scanner.nextInt();

        System.out.println("üçgenin 2.kenarını giriniz:");
        int b=scanner.nextInt();

        System.out.println("üçgenin 3.kenarını giriniz:");
        int c=scanner.nextInt();

        if (a+b>c && b+c>a &&a+c>b ){
            if (a==b && b==c){
                System.out.println("Eskenar ucgendir");
            } else if (a!=b && a!=c && b!=c) {
                System.out.println("Cesitkenar ucgendir.");
            }else System.out.println("Ikizkenar ucgendir.");
          float cevre=a+b+c;
          float s=cevre/2;
          float alan= (float) Math.sqrt(s*(s-a)*(s-b)*(s-c));
            System.out.println("cevre = " + cevre);
            System.out.println("alan = " + alan);


        } else System.out.println("üçgen oluşturmuyor");


          // Soru 18:Girilen sayının Tau sayı olup olmadığını ekrana yazan programı Java dilinde yazınız.

        System.out.println("bir sayı giriniz:");
        int sayı =scanner.nextInt();
        int adet=0;

        for (int i = 1; i <sayı ; i++) {
           if (sayı%i==0){
               adet++;
           }
        }
        System.out.println("adet = " + adet);
        int Tau=sayı%adet;
        if (Tau==0){
            System.out.println("girilen sayı bir tau sayıdır.");
        }else System.out.println("tau sayı değildir.");

 Soru 19:Girilen aralıklardaki heterometrik sayıları ekrana yazan programı Java dilinde yazınız.
   System.out.println("alt değer giriniz:");
      int alt=scanner.nextInt();
      System.out.println("üst değer giriniz:");
      int üst=scanner.nextInt();
      while (alt<üst){
        System.out.println(alt*(alt+1));
        alt++;

      } 12321
      Soru 20:Girilen bir sayının palidrom sayı olup olmadığını ekrana yazan programı Java dilinde yazınız.

        int yenisayı=0;
        System.out.println("bir sayı giriniz:");
        int sayı =scanner.nextInt();
        int x=sayı;
        while (x!=0){
            int kalan=x%10;
            yenisayı=yenisayı*10+kalan;
            x=(x-kalan)/10;
        }
        if (sayı==yenisayı){
            System.out.println("polidrom sayıdır");
        }else System.out.println("Polidrom sayı değildir.");


Örnek 21:İki sayının toplamını hesaplayan ve sonucu ekrana yazan programı Java dilinde yazınız.
System.out.println("1. sayıyı giriniz:");
        int a=scanner.nextInt();
        System.out.println("2. sayıyı giriniz:");
        int b=scanner.nextInt();
        int toplam=a+b;
        System.out.println("toplam = " + toplam);

         // Örnek 22:Kullanıcının girdiği bir sayının kare, küpü ve karekökünü hesaplayan ve bunları ekrana yazan programı Java dilinde yazınız.

        System.out.println("1. sayıyı giriniz:");
        int a=scanner.nextInt();
        int kare= (int) Math.pow(2,a);
        System.out.println("kare = " + kare);
        int küp= (int) Math.pow(3,a);
        System.out.println("küp = " + küp);
        int karekök= (int) Math.sqrt(a);
        System.out.println("karekök = " + karekök);

        Örnek 23:Klavyeden bilinmeyen miktarda okutulan pozitif ve negatif tam sayıların ayrı ayrı toplamlarını, pozitif ve negatif sayıların adetlerini bulan ve sıfır sayısı girildiğinde programı sonlandıran sonuçları ekrana yazan programı Java dilinde yazınız.
          float ptop=0, ntop=0;
       int padet=0,nadet=0;
       int sayı ;


       do {
           System.out.println("sayı giriniz :");
           sayı=scanner.nextInt();
           if (sayı>0){
               ptop+=sayı;
               padet++;
           }
           if (sayı<0){
               ntop+=sayı;
               nadet++;
           }

       }while (sayı!=0);

        System.out.println("padet = " + padet);
        System.out.println("ptop = " + ptop);
        System.out.println("nadet = " + nadet);
        System.out.println("ntop = " + ntop);

 */

    }
}



